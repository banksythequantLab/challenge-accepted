# Pinned to what was actually installed and tested on 2026-08-10.
google-adk==2.6.3
google-genai==2.17.0
google-cloud-firestore>=2.19.0
# Only imported when AGENT_ENGINE_ID is set: VertexAiMemoryBankService does
# `import vertexai` inside __init__ and raises missing_extra without it.
google-cloud-aiplatform>=1.126.0
# Imported lazily by ADK, and ONLY when trace_to_cloud=True. Absent, the very first
# deploy with CA_TRACE_TO_CLOUD=1 dies at import with ModuleNotFoundError before the
# port is bound -- which is exactly how revision 00014 failed.
opentelemetry-exporter-gcp-trace>=1.9.0
# Google Sign-In. Imported lazily by services/auth.py and only when CA_AUTH=required,
# so the test suite and every offline check still run without it -- but it must be in
# the image, or the first authenticated request 500s instead of 401ing.
firebase-admin>=6.6.0
fastapi==0.141.1
uvicorn[standard]==0.46.0
pydantic==2.13.3

# dev
pytest>=8.0
pytest-asyncio>=0.24
